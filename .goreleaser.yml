project_name: k3d-as-a-service

builds: []

checksum:
  name_template: 'checksums.txt'

snapshot:
  name_template: "{{ .Tag }}-next"

brews:
  - tap:
      owner: pthomison
      name: homebrew-tools

    dependencies:
      - name: k3d

    # install: |
    #   prefix.install_symlink "file.service" => "#{service_name}.service"

    # service: |
    #   /Users/pthomison/Projects/kubernetes/flux-environments

    # post_install: |
    #   (var/"lib/example-goreleaser-service").mkpath

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'

before:
  hooks:
    - go mod tidy
    - go fmt ./...

archives:
  - replacements:
      darwin: Darwin
      linux: Linux
      windows: Windows
      386: i386
      amd64: x86_64